# ui 模块开发文档（规划中）

`js/ui/` 用于沉淀可复用 UI 组件逻辑，目前大部分 UI 代码仍在 `index.html` 与 `CardUI` 中。

## 未来拆分方向

- HUD 组件
- Dialog 组件
- Toast / Message 组件
- 版本角标组件
- 战斗意图与敌方状态组件（规划）
- 回合与资源状态组件（规划）

## 拆分原则

- 组件只处理展示和交互，不直接改游戏状态
- 状态变化通过 `EventBus` 或回调注入
- 避免把业务逻辑塞入 DOM 事件处理器
- 怪物化扫雷阶段优先保障信息可读性（危险格、敌方意图、当前可行动作）
